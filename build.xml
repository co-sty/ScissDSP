<?xml version="1.0" ?>
<!--                            -->
<!-- ANT BUILD FILE FOR NETUTIL -->
<!-- requires: Ant 1.6.5+ -->
<!--                            -->
<project name="ScissDSP" default="main">
	<property name="src.dir"		value="src"/>
	<property name="build.dir"		value="build"/>
	<property name="classes.dir"	value="${build.dir}/classes"/>
	<property name="jar.dir"		value="${build.dir}"/>
    <property name="main-class"		value="de.sciss.dsp.ScissDSP"/>
    <property name="version"		value="0.10"/>
    <property name="debug"			value="false"/>
	
	<target name="clean">
		<delete dir="${build.dir}" />
	</target>
	<target name="compile">
		<mkdir dir="${classes.dir}" />
		<javac debug="${debug}" srcdir="${src.dir}" destdir="${classes.dir}" source="1.5" target="1.5" />
	</target>
	<target name="jar" depends="compile">
		<mkdir dir="${jar.dir}" />
		<jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}">
			<manifest>
				<attribute name="Main-Class" value="${main-class}" />
			</manifest>
		</jar>
	</target>
	<target name="main" depends="clean,jar" />
</project>